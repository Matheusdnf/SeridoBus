// Caroneiro (sem registro no sistema)
export class Hitchhiker {
  constructor(
    public id: number,
    public name: string,
    public destination: string,
    public pin: string,
    public passemgersList_id: number
  ) {}
}
